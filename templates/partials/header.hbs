<header>
    <h1 itemprop="name">{{profile.name}}</h1>
    <p itemprop="jobTitle" style="font-size: 1.25rem; margin-bottom: 1rem; font-weight: 500;">
        {{profile.title}}
    </p>
    <p style="color: var(--secondary-text); margin-bottom: 2rem;">
        {{profile.location}} • {{profile.status}}
    </p>
</header>
